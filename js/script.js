const hamburger = document.querySelector('.navigation_btn');
const menu = document.querySelector('.navigation_list');

	hamburger.addEventListener('click', () =>{
    	menu.classList.toggle('menu-open');
	});
	
